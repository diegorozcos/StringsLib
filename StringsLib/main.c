#include <stdio.h>
#include "Library.h"
#include<string.h>

int main() {

    // si se pasa 1 hará WordsReverse()
    // si se pasa 2 hará ReverseSomeWords()
    Reverse_String(1); // aqui se pone la opción

    return 0;
}
